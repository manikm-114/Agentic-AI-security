# agent_history_bench

Offline simulator for tool-using agents with:
- permission scopes
- policy gating
- tamper-evident (hash-chained) execution logs
- history integrity verification
- scenario-driven evaluation

## Quick start

```bash
python src/main.py
```

Outputs:
- outputs/traces/*.json
- outputs/logs/*.json
- outputs/tables/*.csv
